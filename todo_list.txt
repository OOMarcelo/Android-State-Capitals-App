Next:
-implement game logic first (before making the game look appealing)
(see Game Play section below)
-get image of each state
-high score (see final chapter of that book)
    --cite the book that helped me do this in readme

Game Play:
-begin by restarting game; GameActivity.restart()
    --restart the score (UI-wise and through GameActivity.mScore)
    --make GameActivity.currentState and GameActivity.correctCapital both null
    --GameActivity.presentNextState()
-GameActivity.presentNextState()
    --GameActivity.setFourAnswers()
    --set GameActivity.mWaitingForAnswer to true
-GameActivity.setFourAnswers()
    --get a state-capital pair in GameActivity.mStateCapitalMap
        --note to GameActivity that this is the answer
            --e.g. GameActivity.correctState and GameActivity.correctCapital
    --get a general state-capital map, delete the recently selected
        state-capital pair, and randomly pick three answers from this
    --update UI (randomize for the buttons)
        --update the four buttons
        --update the question
-GameActivity.onClick()
    --consider using UI test
    --if GameActivity.mWaitingForAnswer == true:
        --set GameActivity.mWaitingForAnswer to false
        --if (isCorrectCapital(textOfTheButton)):
            --respondToCorrectAnswer()
        --else:
            --respondToWrongAnswer()
    --else:
        --do nothing
-GameActivity.isCorrectCapital(givenCapital):
    --return givenCapital == this.correctCapital
-GameActivity.respondToCorrectAnswer():
    --update GameActivity.mScore and UI score element
    --update GameActivity.mStateCapitalMap
    --if (GameActivity.mScore < 50):
        --GameActivity.presentNextState()
    --else
        --GameActivity.respondToVictory()
-GameActivity.respondToWrongAnswer():
    --GameActivity.restart()
-GameActivity.respondToVictory():
    --go to VictoryActivity
-VictoryActivity:
    --tell the player that he/she has won
    --have a restart button that returns to GameActivity and thus
    restarts the game

Considerable:
-making the states and capitals into enumerators